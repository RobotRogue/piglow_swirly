import parser2, time

#import modules
x=1
#set variable
while True:
    parser2.mail(0)
    time.sleep(10)
#re-run program to infinity, every ten seconds.
#Change the "time.sleep(seconds to wait) variable if you want a longer/shorter check interval. 
